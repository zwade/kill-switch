{
    "private": true,
    "scripts": {
        "build": "bash -c 'cd common && yarn build' && bash -c 'cd content-script && yarn build' && bash -c 'cd popup && yarn build'"
    },
    "workspaces": [
        "*"
    ]
}