{
    "name": "@killswitch/index",
    "scripts": {
        "package": "rm -rf package && mkdir -p package && rsync -rR manifest.json images js/common/dist js/content-script/dist js/popup/dist package"
    }
}